// ToastMessage.jsx
import React from "react";

const ToastMessage = ({ message }) => {
  return (
    <div className="toast">
      {message}
    </div>
  );
};

export default ToastMessage;  // 🔹 Muy importante

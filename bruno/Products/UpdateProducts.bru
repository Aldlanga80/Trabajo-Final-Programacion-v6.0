meta {
  name: "UpdateProduct"
  type: "http"
}

request {
  method: "PATCH"
  url: "{{baseUrl}}/products/{{productId}}"
  headers {
    Authorization: "Bearer {{token}}"
  }
  body {
    type: "json"
    content: {
      "name": "Nuevo nombre",
      "price": 1500
    }
  }
}
